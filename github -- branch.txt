ve en que rama estas
> git branch

crea un nuevo branch
>git checkout -b activos



cuando es un nuevo 
siepre tienes que crear el entorno virtual 
> py -m venv .venv

luego los requierminto
pip install -r requirements.txt

ya luego lo corres
>uvicorn app.main:app --reload